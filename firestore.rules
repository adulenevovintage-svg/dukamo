rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /menu/{document=**} {
      allow read: if true;
      allow write: if true; // Simple public access as requested for a password-protected frontend
    }
  }
}
